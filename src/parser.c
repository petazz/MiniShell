/*#include "minishell.h"

void    tokens(t_msh *msh)
{

}

void    check_lexer(t_msh *msh)
{
    int i;
    int j;

    i = 0;
    j = 0;
    while(msh->prompt[i])
    {
        if (msh->prompt[i] == ' ')
            i++;
        else if (msh->prompt[i] >= 'a' && msh->prompt <= 'z'
            || msh->prompt[i] >= 'A' && msh->prompt[i] <= 'Z')
            msh;

    }

}
*/